# configs/defaults/training_data.yaml
# Defines default settings for training data loading.

training_data:
  symbol: "BTCUSDT" # Default trading symbol for training
  start_date: "2025-01-01 00:00:00" # Default start date for training data (YYYY-MM-DD HH:MM:SS)
  end_date: "2025-03-31 23:59:59" # Default end date for training data (YYYY-MM-DD HH:MM:SS)
  timeframe: "1h" # Default K-line interval for training (e.g., 1m, 5m, 1h, 1d) - Changed to 1h
